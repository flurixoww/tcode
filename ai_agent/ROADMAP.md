1. Fix the file choice algorithm 
  -Add possibility of directly choosing the file
    We do this using @, we need to make an alogrithm that will directly pass the file no matter what to the model if user does this. If the file is already chosen, we skip it in the RAG model.

2. Make a function that will shorten the code
3. Fix file choice algorithm
